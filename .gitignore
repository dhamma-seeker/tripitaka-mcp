# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# Environment variables
.env
.env.*
!.env.example

# Docker
postgres_data/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local-only working docs (Claude + user; never push)
PROGRESS.md
CLAUDE.md
.claude/

# Scratch / throwaway verify scripts + logs (local-only)
scratch/

# MCP App UI build (mcp_app_viewer_html.py = committed artifact; these are not)
mcp_app_ui/node_modules/
mcp_app_ui/dist/

# Data files (large files — manage separately if needed)
# data/*.json
# data/*.csv
data/bilara-data/

# Model cache
model_cache/

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
*.tfvars.json
.terraform.lock.hcl

# Database Dumps
*.dump
*.sql.gz

# SQLite databases (built locally, shipped via HuggingFace — never in git)
tripitaka.db
*.sqlite
*.sqlite3
