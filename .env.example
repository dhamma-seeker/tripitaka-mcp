# =============================================================================
# Tripitaka MCP Server — Environment Variables
# =============================================================================
# คัดลอกไฟล์นี้เป็น .env แล้วแก้ไขค่าตามต้องการ
# cp .env.example .env

# --- Database ---
DATABASE_URL=postgresql://admin:password123@localhost:5432/tripitaka_db

# --- Embedding Model ---
# ชื่อ model จาก HuggingFace Hub สำหรับ sentence-transformers
# แนะนำเริ่มต้น: paraphrase-multilingual-MiniLM-L12-v2 (เบา, 118M)
# ตัวเลือกอื่น: jina-embeddings-v5-text-small (ดีกว่า, 596M)
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
EMBEDDING_DIMENSIONS=384

# --- MCP Server ---
MCP_TRANSPORT=stdio
# MCP_HOST=0.0.0.0
# MCP_PORT=8000

# --- Data ---
# Path สำหรับเก็บ bilara-data (clone จาก SuttaCentral)
BILARA_DATA_PATH=./data/bilara-data
